<header> 
	<% unless current_user.nil? %>
		Welcome <%= current_user.name.to_s  %>
		<br>
		<%= link_to 'Log out', log_out && root_path %>
	<% else %>
		<%= link_to 'Log in', login_path %>
		<br>
		<%= link_to 'Sing Up', users_path %>
	<% end %>
</header>